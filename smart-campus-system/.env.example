# Smart Campus Management System - Environment Configuration

# Database Configuration
MYSQL_ROOT_PASSWORD=smartcampus123
DB_HOST=localhost

# Keycloak Configuration
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin
KEYCLOAK_DB_PASSWORD=smartcampus123

# Application Configuration
SPRING_PROFILES_ACTIVE=docker
JWT_ISSUER_URI=http://keycloak:8080/realms/smartcampus

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://localhost:8080

# Notification Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_ENABLE_TLS=true

# Redis Configuration (Optional for caching)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Logging Configuration
LOG_LEVEL=INFO
ROOT_LOG_LEVEL=WARN

# Security Configuration
JWT_SECRET=SmartCampusSecretKey2024!@#$%^&*()
SESSION_TIMEOUT=30

# File Upload Configuration
MAX_FILE_SIZE=10MB
MAX_REQUEST_SIZE=10MB
UPLOAD_PATH=/app/uploads

# Monitoring Configuration
ACTUATOR_ENDPOINTS_ENABLED=true
METRICS_EXPORT_ENABLED=false

# Production Specific (Set these in production)
# MYSQL_ROOT_PASSWORD=<secure-random-password>
# KEYCLOAK_ADMIN_PASSWORD=<secure-random-password>
# JWT_SECRET=<256-bit-random-key>
# CORS_ALLOWED_ORIGINS=https://yourdomain.com